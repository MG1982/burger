<div class="mt-4">
    <h4>{{burger_name}}</h4>

    <!--Devour it button-->
    <button class="change-devour btn btn-primary" data-id="{{id}}" data-newdevour="{{devour}}"><i
            class="fas fa-cookie-bite"></i> Devour it
    </button>
    <hr class="my-4">
</div>